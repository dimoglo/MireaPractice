# Валютный компас (MireaPractice)

Мобильное приложение для отслеживания курсов валют, конвертации валют и анализа динамики курсов.

## 📱 Описание проекта

Приложение предоставляет пользователям возможность:
- Просматривать актуальные курсы валют ЦБ РФ
- Конвертировать валюты с помощью удобного калькулятора
- Анализировать динамику курсов валют за выбранный период
- Просматривать новости о валютном рынке
- Находить банки для обмена валют

## 🏗️ Архитектура проекта

Проект построен на основе **Clean Architecture** с использованием паттерна **MVVM** (Model-View-ViewModel).

### Слои архитектуры:

```
┌─────────────────────────────────────┐
│           UI                        │
│  (Screens, ViewModels, Components)  │
└──────────────┬──────────────────────┘
               │
┌──────────────▼───────────────────────┐
│         Domain                       │
│  (Models, Repository Interfaces)     │
└──────────────┬───────────────────────┘
               │
┌──────────────▼───────────────────────┐
│          Data                        │
│  (Repository Impl, API, Database)    │
└──────────────────────────────────-───┘
```

### Основные принципы:

1. **Разделение ответственности**: Каждый слой имеет четко определенную ответственность
2. **Инверсия зависимостей**: UI и Data слои зависят от Domain слоя
3. **Реактивное программирование**: Использование StateFlow для управления состоянием UI
4. **Dependency Injection**: Hilt для управления зависимостями

## 📂 Структура проекта

```
app/src/main/java/com/example/mireapractice/
│
├── common/                          # Общие утилиты и темы
│   ├── theme/                       # Тема приложения
│   │   ├── Color.kt                 # Цветовая палитра
│   │   ├── Theme.kt                 # Настройки темы Material3
│   │   └── Type.kt                  # Типографика
│   └── utils/                       # Утилиты
│       ├── Constants.kt             # Константы приложения
│       └── PopularCurrency.kt       # Утилиты для популярных валют
│
├── data/                            # Data Layer
│   ├── CurrencyRepositoryImpl.kt   # Реализация репозитория валют
│   ├── local/                       # Локальное хранилище (Realm)
│   │   └── CurrencyEntity.kt       # Entity для Realm
│   ├── mapper/                      # Маппинг между слоями
│   │   └── CurrencyMapper.kt        # Конвертация DTO ↔ Entity ↔ Model
│   └── remote/                      # Удаленные API
│       ├── banks/                   # API банков
│       │   ├── BankApiService.kt
│       │   └── BankDto.kt
│       ├── currency/                 # API курсов валют
│       │   ├── CurrencyApiService.kt
│       │   └── CurrencyDto.kt
│       └── news/                    # API новостей
│           ├── NewsApiService.kt
│           └── NewsResponseDto.kt
│
├── domain/                          # Domain Layer
│   ├── CurrencyModel.kt             # Доменные модели
│   └── CurrencyRepository.kt        # Интерфейс репозитория
│
├── di/                              # Dependency Injection
│   └── AppModule.kt                 # Модуль Hilt для DI
│
├── ui/                              # UI Layer
│   ├── analytics/                   # Экран аналитики
│   │   ├── AnalysisScreen.kt
│   │   └── AnalysisViewModel.kt
│   ├── banks/                       # Экран обмена в банках
│   │   └── ExchangeInBanksScreen.kt
│   ├── calculator/                  # Экран калькулятора
│   │   ├── CalculatorScreen.kt
│   │   └── CalculatorViewModel.kt
│   ├── components/                  # Переиспользуемые компоненты
│   │   ├── arrows/                  # Стрелки (вверх/вниз)
│   │   ├── banner/                  # Баннеры новостей
│   │   ├── bottombar/               # Нижняя навигационная панель
│   │   ├── currency_cards/          # Карточки валют
│   │   └── navbar/                  # Верхняя навигационная панель
│   ├── home/                        # Главный экран
│   │   ├── HomeScreen.kt
│   │   └── HomeViewModel.kt
│   ├── news/                        # Экран новостей
│   │   ├── NewsScreen.kt
│   │   └── NewsViewModel.kt
│   └── notifications/               # Экран уведомлений
│       └── NotificationsScreen.kt
│
├── MainActivity.kt                  # Главная активность
└── MireaPracticeApplication.kt       # Application класс
```

## 🎯 Основные компоненты и классы

### Domain Layer

#### `CurrencyModel`
Доменная модель валюты, содержащая:
- `charCode` - буквенный код валюты (USD, EUR и т.д.)
- `numCode` - числовой код валюты
- `name` - название валюты
- `value` - текущий курс
- `nominal` - номинал
- `previous` - предыдущий курс
- `difference` - разница с предыдущим курсом
- `percentChange` - процент изменения
- `isGrowing` - направление изменения (рост/падение)

#### `ExchangeRateModel`
Модель курсов валют на определенную дату:
- `date` - дата курсов
- `timestamp` - временная метка
- `currencies` - список валют
- `updateAt` - время обновления

#### `CurrencyRepository`
Интерфейс репозитория для работы с курсами валют:
- `fetchAndCacheExchangeRate()` - получение свежих курсов из API и кэширование
- `getCachedExchangeRate()` - получение последних кэшированных курсов
- `getExchangeRateByDate(date)` - получение курсов по конкретной дате
- `getPopularCurrencies()` - получение популярных валют
- `getAllCurrencies()` - получение всех валют
- `getExchangeRatesByDateRange()` - получение курсов за период

### Data Layer

#### `CurrencyRepositoryImpl`
Реализация репозитория валют:
- Получение данных из API через Retrofit
- Кэширование данных в Realm
- Маппинг DTO в Entity и Entity в Model
- Обработка ошибок и fallback на кэш

#### `CurrencyMapper`
Утилиты для маппинга между слоями:
- `CurrencyDto.toEntity()` - конвертация DTO в Entity для Realm
- `CurrencyEntity.toModel()` - конвертация Entity в Domain Model
- Вычисление разницы, процента изменения и направления тренда

#### `CurrencyEntity` / `ExchangeRateEntity`
Realm Entity для локального хранения:
- Используют составные ключи для уникальности
- Хранят BigDecimal значения как String (ограничение Realm)

### UI Layer

#### ViewModels

**`HomeViewModel`**
- Управляет состоянием главного экрана
- Загружает курсы валют и новости параллельно
- Обрабатывает выбор даты через DatePicker
- Сортирует валюты по популярности
- Обрабатывает ошибки сети с fallback на кэш

**`CalculatorViewModel`**
- Управляет состоянием калькулятора валют
- Выполняет конвертацию между валютами через RUB
- Обрабатывает ввод с клавиатуры
- Управляет выбором валют через диалог

**`AnalysisViewModel`**
- Управляет состоянием экрана аналитики
- Загружает данные за выбранный период
- Вычисляет min/max значения для графика
- Обрабатывает выбор валюты и дат

**`NewsViewModel`**
- Управляет состоянием экрана новостей
- Загружает список новостей

#### Screens

**`HomeScreen`**
- Отображает список валют с карточками
- Показывает одну новость в виде баннера
- Содержит DatePicker для выбора даты
- Навигация к экранам новостей и уведомлений

**`CalculatorScreen`**
- Отображает поля ввода/вывода валют
- Клавиатура для ввода чисел
- Диалог выбора валют
- Реализует конвертацию в реальном времени

**`AnalysisScreen`**
- Отображает график динамики курса
- Селекторы дат (от/до)
- Селектор валюты
- Метрики (min/max значения)

**`NewsScreen`**
- Список всех новостей
- Разделители между новостями

**`ExchangeInBanksScreen`**
- WebView с сайтом banki.ru
- Скрытие брендинга через JavaScript injection

**`NotificationsScreen`**
- Экран уведомлений (заглушка)

#### Components

**`NavBar`**
- Переиспользуемая верхняя навигационная панель
- Поддержка левого, центрального и правого контента
- Кастомная система измерения для гибкой компоновки

**`BottomBar`**
- Нижняя навигационная панель
- Иконки для основных разделов приложения
- Подсветка активного раздела

**`CurrencyCard`**
- Карточка отображения валюты
- Флаг страны, название, курс
- Индикатор изменения (стрелка вверх/вниз)
- Цветовая индикация роста/падения

**`Banner`**
- Баннер для отображения новостей
- Заголовок, подзаголовок, изображение

**`ArrowUp` / `ArrowDown`**
- Кастомные стрелки для индикации направления изменения курса

## 🔧 Технологический стек

### Основные библиотеки:

- **Jetpack Compose** - декларативный UI фреймворк
- **Material 3** - дизайн система
- **Hilt** - Dependency Injection
- **Kotlin Coroutines** - асинхронное программирование
- **Flow** - реактивные потоки данных
- **Retrofit** - HTTP клиент для API запросов
- **OkHttp** - HTTP клиент с логированием
- **Realm** - локальная база данных
- **Coil** - загрузка изображений
- **MPAndroidChart** - графики для аналитики
- **Timber** - логирование

### Архитектурные компоненты:

- **ViewModel** - управление UI состоянием
- **StateFlow** - реактивное состояние
- **Repository Pattern** - абстракция данных
- **Clean Architecture** - разделение на слои

## 🔄 Поток данных

```
User Action
    ↓
Screen (Composable)
    ↓
ViewModel (обработка события)
    ↓
Repository (получение данных)
    ↓
API / Database
    ↓
Repository (маппинг данных)
    ↓
ViewModel (обновление StateFlow)
    ↓
Screen (реактивное обновление UI)
```

## 📊 Основные функции

### 1. Просмотр курсов валют
- Загрузка актуальных курсов из API ЦБ РФ
- Кэширование в локальной БД (Realm)
- Отображение популярных валют первыми
- Выбор даты через DatePicker
- Индикация изменения курса (рост/падение)

### 2. Конвертация валют
- Выбор исходной и целевой валюты
- Ввод суммы через цифровую клавиатуру
- Конвертация через RUB как базовую валюту
- Отображение результата в реальном времени

### 3. Аналитика курсов
- Выбор периода (от/до даты)
- Выбор валюты для анализа
- Построение графика динамики курса
- Отображение min/max значений

### 4. Новости
- Загрузка новостей о валютном рынке
- Отображение на главном экране (одна новость)
- Полный список на отдельном экране

### 5. Обмен в банках
- WebView с сайтом banki.ru
- Скрытие брендинга через JavaScript injection
- Отображение только информации о курсах

## 🚀 Навигация

Приложение использует простую навигацию на основе состояния:

- **Главный экран** - точка входа
- **Уведомления** - доступ через иконку колокольчика
- **Новости** - доступ через баннер на главном экране
- **Обмен в банках** - через нижнюю панель
- **Калькулятор** - через нижнюю панель
- **Аналитика** - через нижнюю панель

Навигация реализована через `enum Screen` и `remember { mutableStateOf()}` в `MainActivity`.

## 💾 Хранение данных

### Локальное хранилище (Realm)
- Курсы валют кэшируются по датам
- Используются составные ключи для уникальности
- Автоматическое удаление старых данных при обновлении

### Структура данных:
- `ExchangeRateEntity` - курсы на дату
- `CurrencyEntity` - отдельная валюта с привязкой к дате

## 🎨 UI/UX

- **Material 3** дизайн система
- Кастомная цветовая палитра
- Адаптивная типографика
- Поддержка темной темы (через Material 3)
- Плавные анимации переходов
- Интуитивная навигация

## 📝 Примечания

- Приложение обрабатывает ошибки сети и использует кэш при недоступности API
- Новости загружаются с fallback на моки при ошибках
- Все операции с сетью выполняются асинхронно через Coroutines
- UI обновляется реактивно через StateFlow

## 🔐 Разрешения

- `INTERNET` - для загрузки данных из API
- `ACCESS_NETWORK_STATE` - для проверки доступности сети

## 📱 Минимальные требования

- **minSdk**: 26 (Android 8.0)
- **targetSdk**: 36
- **compileSdk**: 36

## 🛠️ Сборка проекта

1. Клонировать репозиторий
2. Открыть проект в Android Studio
3. Синхронизировать Gradle
4. Запустить приложение на устройстве/эмуляторе

## 📄 Лицензия

Проект создан в учебных целях.

